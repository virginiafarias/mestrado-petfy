<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cap7.petfy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AnimalCadastroActivity"
            android:label="@string/animal_cadastro_label"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="Início"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AnimalListagemActivity"
            android:label="@string/animal_listagem_label"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AnimalDetalheActivity"
            android:label="@string/title_activity_animal_detalhe"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddVacinaActivity"
            android:label="@string/title_activity_add_vacina"
            android:parentActivityName=".AnimalDetalheActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="cap7.petfy.AnimalDetalheActivity" />
        </activity>
    </application>

</manifest>
